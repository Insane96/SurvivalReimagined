
#Wear down grass when passing on it.
["Desire Paths"]
	#Chance for blocks to transform each tick
	#Range: 0.0 ~ 1.0
	"Chance to transform" = 0.008
	#If true, speed will increase/decrease based off speed. The base chance 'Chance to transform' is when you walk.
	"Speed based chance" = true
	#Tall grass is broken when grass is transformed
	"Break Tall Grass" = true
	#Transform list of blocks
	"Transformation List" = ["minecraft:grass_block,minecraft:dirt", "minecraft:dirt,minecraft:coarse_dirt"]
	#Each level of Feather Falling will reduce the chance to transform by this percentage amount. E.g. with the default settings, Feather Falling IV will fully prevent blocks from transforming.
	#Range: 0.0 ~ 1.0
	"Feather falling transform chance reduction" = 0.25
	#If true, crouching will prevent blocks from transforming.
	"Crouch prevents transform" = true

